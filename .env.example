# Copy this file and rename it to ".env", then fill in your real key.
# NEVER commit the real .env file to GitHub.

# Google Cloud Translation API key (optional).
# If left empty, the app automatically uses the free MyMemory API.
GOOGLE_TRANSLATE_API_KEY=your_google_translate_api_key_here

# Flask settings
FLASK_DEBUG=True
PORT=5000
